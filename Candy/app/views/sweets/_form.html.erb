

<%= simple_form_for @sweet do |f| %>
  <% if @sweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sweet.errors.count, "error") %> prohibited this sweet from being saved:</h2>

      <ul>
      <% @sweet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.input :name, class: "form-control" %><br>

    <%= f.input :photo, class: "form-control" %><br>

    <%= f.association :category %>

    <%= f.hidden_field :user  %>
  </div>

  <br>
  <div class="actions">
    <%= f.button :submit, class:"btn btn-primary btn-lg"%>

  </div>

  <%end%>
